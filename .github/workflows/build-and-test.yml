name: Build and test

on:
  push:
    branches:
      - "main"
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '35 4 * * TUE' # Tuesday at 04:35 UTC”

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: ${{ matrix.os }} py${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}-latest
    defaults:
      run:
        shell: ${{ matrix.shell }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13']
        os: [Ubuntu, macOS, Windows]
        include:
          - os: Ubuntu
            environment-file: ci/requirements-linux.yml
            shell: bash -l {0}
          - os: macOS
            environment-file: ci/requirements-macos.yml
            shell: bash -l {0}
          - os: Windows
            environment-file: ci/requirements-windows.yml
            shell: powershell

    steps:
      - uses: actions/checkout@v6
      - uses: conda-incubator/setup-miniconda@v4
        with:
          miniconda-version: "latest"
          python-version: ${{ matrix.python-version }}
          activate-environment: sbm_build_env
          channel-priority: strict
          environment-file: ${{ matrix.environment-file }}

      - name: Configure PATH for Windows Fortran build
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          # gfortran comes from C:\mingw64\bin (GHCup's MinGW-Builds 15.x).
          # conda's Library\mingw-w64\bin ships older m2w64 runtime DLLs that cause
          # STATUS_ENTRYPOINT_NOT_FOUND (0xC0000139) when gfortran's sanity-check
          # binary runs. Remove all competing MinGW DLL directories, then put
          # C:\mingw64\bin first so the correct runtime DLLs are found.
          $mingwBin = "C:\mingw64\bin"
          $filteredPath = ($env:PATH -split ';' | Where-Object {
              $_ -notlike 'C:\Strawberry*' -and
              $_ -notlike '*Library\mingw-w64\bin' -and
              $_ -notlike 'C:\Program Files\Git\mingw64*' -and
              $_ -ne ''
          }) -join ';'
          "PATH=$mingwBin;$filteredPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "FC=$mingwBin\gfortran.exe" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "CC=$mingwBin\gcc.exe" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build and install
        run: |
          python -m pip install --no-build-isolation --no-deps .
      - name: Run tests
        run: |
          pytest -v --pyargs climlab_sbm_convection
